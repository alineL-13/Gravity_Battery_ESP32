# Gravity_Battery_ESP32
The ESP32 reads a voltage sensor while a weight descends, sends the data to a local web server, and displays the generated voltage in real time. All measurements are stored in a binary tree with timestamps, and the web interface shows a voltage-over-time chart.
